<%- include("partials/header") %> 

    <h1>Home</h1>

    <p> <%= HomeText  %> </p>

    <% for (var i = posts.length - 1; i >= 0; i--) {  %> 
        <a href="/posts/ + <%= posts[i].title %>" style="text-decoration: none; color: black;"> <h1> <%= posts[i].title %>  </h1> </a>
        <p> <%= posts[i].text.slice(0, 150) + " ..." %> 
            <a href="/posts/ + <%= (posts[i].title)%>">Read more</a>
        </p>
    <% };  %> 

<%- include("partials/footer") %>